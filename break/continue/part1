#include <stdio.h>

int main()
{
    int num,sum=0,c=1;
    while(c<11)
{
printf("Enter the number\n");
scanf("%d",&num);
if(num<=0)
{
    break;
}
else
{
    sum=num+sum;
     c++;
    continue;
    
}
}
 printf("sum of num is =%d",sum);  
}
